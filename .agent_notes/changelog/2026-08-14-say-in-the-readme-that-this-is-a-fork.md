# 2026-08-14 - Say in the README that this is a fork

**Why:** The README was still upstream's, so nothing on the repo's front page
said whose fork this is, what it adds, or how to install it. Same problem as
the manifest author line, one level up.

**What:** A short "This fork" section above the upstream README: who forked it
and from where, that it installs beside upstream under the `kanban-custom` id,
the two user-facing additions (auto-move completed cards to the done list,
move a card to another board), and the BRAT install line. Upstream's README is
left in place underneath.
